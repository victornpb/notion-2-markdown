{
  "name": "e2e",
  "version": "0.0.0",
  "description": "End to End Testing for development",
  "main": "index.js",
  "type": "module",
  "scripts": {
    "start": "npm --prefix ../ run build && node .",
    "env": "cp -i .env.example .env"
  },
  "dependencies": {
    "@notionhq/client": "^1.0.4",
    "dotenv-safe": "^8.2.0"
  },
  "engines": {
    "node": ">=12.0.0"
  }
}
